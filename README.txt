ВОПРОСЫ:
1. Указал базовый URL в фикстуре, в самом тесте указал отрносительный путь, но почему то браузер не хотел открываться на нужной странице
Заработало только когда указал абсолютный путь в теле самого теста

Хотелось бы разобраться в чём причина, возможно для этого надо использовать selenium, я не импортировал его


2. Для выбора гендеро использовал следующий код

browser.element('#gender-radio-1').click()

На что выдавал ошибку, удалось расшифровать ее так:
Ошибка ElementClickInterceptedException указывает на то, что элемент,который вы пытаетесь кликнуть, перекрыт другим элементом.
В данном случае, элемент <input> перекрыт элементом <label>.

Визуально вроде бы не видно что бы радиобаттон закрывало название радиобаттона

ГПТ помог следующим кодом browser.element('label[for="gender-radio-1"]').click()

Хотелось бы понять как определять такие моменты (что элемент может быть перекрыт, когда изуально этого не видно), если это возможно
и как находить решение проблемы без ГПТ?

2. Такая же проблема была с кликами по чекбоксам

3. Использовал следующий код для проверки то что чекбоксы выбраны
    browser.all('[type="checkbox"]')[1].should(have.attribute('value').value('2'))
    browser.all('[type="checkbox"]')[2].should(have.attribute('value').value('3'))
   Во первых, не совсем понятно для чего такая проверка (я подсмотрел их у другого студента)
   ВО вторых удалось их написать, чтобы заработали только с помощью ГПТ
   Нужно понимание как обращаться к нескольким элементам, по индексу или иным способом

4. Почему после прохождения теста, выдает ошибку, что тест функция возвращает None вместо test, вообще не понятно откуда это взялось?


